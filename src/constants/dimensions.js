import { Dimensions } from 'react-native';

// --- Dimensions ---
export const { width: windowWidth } = Dimensions.get('window');

// You could add other dimension-related constants here
// export const windowHeight = Dimensions.get('window').height;
